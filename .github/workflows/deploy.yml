name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Trigger Render Redeploy
    runs-on: ubuntu-latest
    steps:
      - name: Send Webhook to Render
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
